/** @type {import('tailwindcss').Config} */
module.exports = {
  mode: "jit", // JIT (just-in-time) mode を適用
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
